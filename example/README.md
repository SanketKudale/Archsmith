# Example

Create a sample project from the package root:

```shell
fvm dart run ../bin/archsmith.dart create example_app --architecture clean_feature --state riverpod --router go_router --network dio --runtime-protection standard
```

Run with `--dry-run` to preview the generated paths without creating the Flutter project.
